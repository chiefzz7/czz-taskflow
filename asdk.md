# Supabase Configurado com Sucesso

As credenciais do Supabase foram migradas para o arquivo `.env` do backend com segurança:
- **Banco de Dados:** Conectado via pooling PostgreSQL do Supabase (`aws-0-us-east-1.pooler.supabase.com:6543`).
- **Storage:** Bucket `taskflow-media` público configurado com a chave `service_role`.